# --- Django core ---
DEBUG=true
DJANGO_SECRET_KEY=dev-secret-change-me
ALLOWED_HOSTS=127.0.0.1,localhost

# --- Database (local fallback: sqlite; comment this if using Cloud SQL proxy) ---
# For Cloud SQL with MySQL, example (when using Cloud SQL Auth proxy):
# DATABASE_URL=mysql://django_user:password@127.0.0.1:3306/django_db
DATABASE_URL=sqlite:///db.sqlite3

# --- Google Cloud Storage bucket for media ---
GS_BUCKET_NAME=funnamics-media
GS_LOCATION=media

# --- Email settings ---
DEFAULT_FROM_EMAIL=support@tbae.co.za
EMAIL_HOST=uk71.siteground.eu
EMAIL_PORT=465
EMAIL_HOST_USER=support@tbae.co.za
EMAIL_HOST_PASSWORD=change-me
EMAIL_USE_TLS=false
EMAIL_USE_SSL=true

# --- CORS / CSRF ---
CORS_ALLOW_ALL=false
CSRF_EXTRA=https://www.tbae.co.za
